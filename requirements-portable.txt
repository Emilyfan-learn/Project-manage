# Minimal dependencies for portable installation
# Uses Pydantic v1 (no compiled dependencies)

# Core web framework
fastapi==0.103.2
uvicorn==0.24.0

# Data validation (v1 - no pydantic-core needed)
pydantic==1.10.13

# Utilities
python-dateutil==2.8.2
python-multipart==0.0.6
