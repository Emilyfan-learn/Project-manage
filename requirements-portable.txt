# Minimal dependencies for portable installation
# CSV import/export works without additional dependencies (Python built-in)
# CSV files can be opened directly in Excel

# Core web framework
fastapi==0.104.1
uvicorn==0.24.0

# Data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Utilities
python-dateutil==2.8.2
python-multipart==0.0.6
